// 统一定义xuperledger对外暴露接口
package xuperledger

import (
	"fmt"
)

type XuperLedger struct {
}

func CreateLedger() (*XuperLedger, error) {
	return nil, fmt.Errrof("The interface has not been implemented")
}
